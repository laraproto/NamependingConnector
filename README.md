# NamependingConnector

This is the connector plugin for Namepending, allowing sync to and from the panel, otherwise the panel is just kind of useless

## Building

This is an SDK style project so should be all and all pretty easy to build, you will need to set up an environment variable called SL_REFERENECS pointing to the dedicated server's managed folder or set up a ReferencePath.props.user file
